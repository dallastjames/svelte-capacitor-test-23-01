import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dallastjames.ionic.testing.sveltetest102',
  appName: 'svelte-cap-test',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
